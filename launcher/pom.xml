<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.destenylp</groupId>
        <artifactId>xBotenyy-parent</artifactId>
        <version>3.0-BETA</version>
    </parent>

    <artifactId>xBotenyyLauncher</artifactId>
    <packaging>jar</packaging>

    <description>
        Startet xBotenyyDiscordBot und xBotenyyTwitchBot gemeinsam in einem Prozess. Enthaelt keine eigene
        Bot-Logik, sondern instanziiert und startet lediglich die bestehenden Bot-Klassen der beiden Module,
        stellt ein je Modul getrenntes Logging-Setup bereit und bietet zusaetzlich eine interaktive Konsole
        (start/stop/restart/set/status/exit) zur Laufzeitsteuerung beider Bots.
    </description>

    <dependencies>
        <dependency>
            <groupId>de.destenylp</groupId>
            <artifactId>xBotenyyCommon</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.destenylp</groupId>
            <artifactId>xBotenyyDiscordBot</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.destenylp</groupId>
            <artifactId>xBotenyyTwitchBot</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>6.1.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>xBotenyyLauncher</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.8.0</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/with-dependencies.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>false</appendAssemblyId>
                    <archive>
                        <manifest>
                            <mainClass>de.destenylp.xBotenyy.launcher.Main</mainClass>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
